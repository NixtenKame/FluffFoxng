<% content_for :page_title, "Fan Art" %>

<div class="static-content fan-art-page">
  <h1>Fan Art</h1>

  <div class="fan-art-description">
    <p>Thank you all to the artists who have worked really hard on these fan art pieces. As an honor, I decided to create a dedicated web page for these wonderful art pieces of my fursona.</p>

    <p>If you have fan art you would like to share, please contact me through Discord or Email.</p>

    <div class="contact-links">
      <%= link_to "Submit Your Fan Art", new_posts_fan_art_path, class: "btn btn-submit" %>
      <%= link_to "Email Me", "mailto:nixtenkame@gmail.com", class: "btn btn-email" %>
      <p>
        <%= link_to "Discord", "https://discordapp.com/users/881220034431713282", target: "_blank", rel: "noopener" %> | 
        <span class="note">Note: if you are contacting me through Discord, please include "FluffFox Fan Art" in the message with the completed artwork. <strong>I WILL NOT ACCEPT ANY FAN ART THAT I HAVE TO PAY FOR AS THAT IS NOT FAIR.</strong></span>
      </p>
    </div>
  </div>

  <section class="fan-art-grid">
    <% if @fan_artworks.any? %>
      <% @fan_artworks.each do |artwork| %>
        <article class="fan-art-card">
          <div class="fan-art-wrapper">
            <% if artwork.featured? %>
              <div class="featured-badge">FEATURED</div>
            <% end %>

            <%= image_tag artwork.image_url, alt: artwork.title, class: "fan-art-image" %>
          </div>

          <div class="fan-art-info">
            <h3 class="fan-art-title"><%= artwork.title %></h3>

            <div class="fan-art-artist">
              <span>Art by</span>
              <% if artwork.artist_url.present? %>
                <%= link_to artwork.artist_name, artwork.artist_url, target: "_blank", rel: "noopener", class: "artist-link" %>
              <% else %>
                <%= artwork.artist_name %>
              <% end %>
            </div>
          </div>
        </article>
      <% end %>
    <% else %>
      <p class="no-artwork">No fan art submissions yet. <%= link_to "Be the first to contribute!", new_posts_fan_art_path %></p>
    <% end %>
  </section>
</div>

<style>
  .fan-art-page {
    padding: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .fan-art-page h1 {
    margin-bottom: 1.5rem;
    font-size: 2.5rem;
  }

  .fan-art-description {
    margin-bottom: 3rem;
    line-height: 1.6;
  }

  .fan-art-description p {
    margin-bottom: 1rem;
  }

  .contact-links {
    margin-top: 1.5rem;
    padding: 1.5rem;
    background: themed("color-section");
    border-radius: 8px;
  }

  .contact-links .btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    color: #fff;
    text-decoration: none;
    border-radius: 6px;
    margin-right: 0.5rem;
    margin-bottom: 1rem;
    font-weight: 600;
  }

  .contact-links .btn:hover {
    opacity: 0.9;
  }

  .btn-submit {
    background: #2ecc71;
  }

  .btn-email {
    background: #3498db;
  }

  .contact-links .note {
    font-size: 0.95rem;
    opacity: 0.9;
  }

  .fan-art-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 1.5rem;
    margin-top: 2rem;
  }

  .fan-art-card {
    background: themed("color-foreground");
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 6px 18px rgba(0, 0, 0, 0.35);
  }

  .fan-art-card:hover {
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.45);
  }

  .fan-art-wrapper {
    position: relative;
    width: 100%;
    height: 260px;
    overflow: hidden;
  }

  .fan-art-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .featured-badge {
    position: absolute;
    top: 10px;
    left: 10px;
    background: #1e90ff;
    color: #fff;
    padding: 0.3rem 0.6rem;
    font-size: 0.75rem;
    border-radius: 6px;
    font-weight: bold;
    z-index: 10;
  }

  .fan-art-info {
    padding: 1rem;
    background: themed("color-background");
  }

  .fan-art-title {
    font-size: 1.1rem;
    font-weight: 600;
    margin: 0 0 0.5rem 0;
    color: themed("color-text");
  }

  .fan-art-artist {
    font-size: 0.9rem;
    opacity: 0.85;
    color: themed("color-text-muted");
  }

  .artist-link {
    color: themed("color-foreground");
    text-decoration: none;
    font-weight: 500;
  }

  .artist-link:hover {
    text-decoration: underline;
  }

  .no-artwork {
    grid-column: 1 / -1;
    text-align: center;
    padding: 2rem;
    color: themed("color-text-muted");
    font-style: italic;
  }

  .no-artwork a {
    color: themed("color-foreground");
    text-decoration: underline;
  }

  @media (max-width: 768px) {
    .fan-art-page {
      padding: 1rem;
    }

    .fan-art-page h1 {
      font-size: 2rem;
    }

    .fan-art-grid {
      grid-template-columns: 1fr;
    }

    .contact-links .btn {
      display: block;
      margin-right: 0;
      margin-bottom: 0.5rem;
    }
  }
</style>

<%= render "secondary_links" %>
